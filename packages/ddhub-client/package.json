{
  "name": "@energyweb/greenproof-ddhub-client",
  "version": "1.0.2",
  "license": "MIT",
  "author": "EnergyWeb DevHub GmbH;",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "yarn generate:client && tsc",
    "lint": "eslint \"{src,test}/**/*{.ts,.tsx}\" --quiet",
    "lint:fix": "yarn lint --fix",
    "test:unit": "echo no unit tests for package",
    "test:e2e": "echo no e2e tests for package",
    "generate:client": "rm -rf src/client && orval && barrelsby -d src/client && (eslint --fix src/client/*.ts || true)"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "zod": "^3.18.0"
  },
  "devDependencies": {
    "barrelsby": "^2.4.0",
    "orval": "^6.9.6"
  }
}
